# Contributing to ZeroGuard

Thanks for your interest! We follow a standard fork & pull request workflow.

## Development Setup
1. Fork and clone the repo.
2. Run `pnpm install` to install dependencies.
3. Run `pnpm build` to compile all packages.
4. Run `pnpm test` to run tests.

## Code Style
- TypeScript strict mode.
- Prettier and ESLint configured.
- Conventional commits.

## Adding a New Rule
See `packages/rules/README.md` (coming soon).
